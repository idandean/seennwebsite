{
  "name": "@seenn/voice-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Build + test toolchain for the public voice-demo widget. Deliberately isolated in tools/ so the repository root stays package.json-free and Vercel keeps treating the site as a plain static deploy.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@typescript-eslint/eslint-plugin": "^8.18.1",
    "@typescript-eslint/parser": "^8.18.1",
    "esbuild": "^0.24.2",
    "eslint": "^9.17.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
